import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.boutiquemanager.app",
  appName: "Boutique Manager",
  webDir: "dist",
};

export default config;
